import type { BuildProvenance } from "@/lib/build-provenance";

type BuildProvenanceProps = {
  buildProvenance: BuildProvenance;
};

export function BuildProvenancePanel({}: BuildProvenanceProps) {
  return null;
}
